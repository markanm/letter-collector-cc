import { PathAnalyzer } from "../components/PathAnalyzer";
import * as Styled from "./Analyzer.Styles";

const Analyzer = () => {
  return (
    <Styled.Container>
      <PathAnalyzer />
    </Styled.Container>
  );
};

export default Analyzer;
